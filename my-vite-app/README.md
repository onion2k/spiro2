# my-vite-app

Vite + React + TypeScript project configured to use Rolldown via:

- `vite`: `npm:rolldown-vite@latest`

## Run

```bash
cd my-vite-app
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```
